extends layout

block content 
    main
        section.heading 
            h2=title
            .buttons 
                a.btn.btn-edit(href="") Edit 
                a.btn.btn-delete(href="") Delete 
        